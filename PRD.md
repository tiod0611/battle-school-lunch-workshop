# PRD (Product Requirements Document)

## 1. 개요

### 1.1 앱 제목

**급식 배틀 - 학교 급식 조회 앱**

### 1.2 개발 목적

전국 학교의 급식(중식) 정보를 손쉽게 조회할 수 있는 웹 애플리케이션을 제공한다. 사용자는 학교 이름을 몰라도 일부 검색어만으로 학교를 찾을 수 있으며, 원하는 날짜 범위의 급식 메뉴를 한눈에 확인할 수 있다. 나아가 전국 학교의 급식을 재미있게 비교해볼 수 있는 "오늘의 왕" 토너먼트 기능을 통해 사용자의 흥미와 재방문을 유도한다.

### 1.3 대상 사용자

- 자녀의 학교 급식 메뉴가 궁금한 학부모
- 본인 학교 및 다른 학교의 급식을 비교해보고 싶은 학생
- 전국 급식 트렌드나 재미 요소(오늘의 왕)에 관심 있는 일반 사용자

### 1.4 데이터 출처

- NEIS(교육정보 공시 서비스)에서 제공하는 공개 API (`data/openapi.json`에 정의된 학교기본정보, 급식식단정보 API)

---

## 2. 사용자 흐름 (User Flow)

앱은 아래 3단계로 구성된 핵심 사용자 흐름을 따른다.

1. **학교 검색**: 사용자가 학교 이름의 일부만 입력하면, 일치하는 학교 목록이 검색되어 표시된다. 사용자는 목록에서 원하는 학교를 선택한다.
2. **날짜 범위 선택**: 사용자가 급식 정보를 조회할 시작일과 종료일을 선택한다.
3. **급식 결과 표시**: 선택한 학교와 날짜 범위에 해당하는 날짜별 중식 급식 메뉴가 표시된다.

```
[학교 검색] → [학교 선택] → [날짜 범위 선택] → [날짜별 중식 급식 결과 표시]
```

---

## 3. 기능 요구사항

### 3.1 학교 검색 및 선택

- 사용자는 학교 이름의 일부(부분 문자열)만으로 학교를 검색할 수 있다.
- 검색 대상 학교급은 제한하지 않는다(초등학교/중학교/고등학교 전체). 단, 3.4절의 "오늘의 왕" 토너먼트는 고등학교로 한정한다.
- 검색 결과는 학교명, 지역(시도교육청), 학교급 등 학교를 구분할 수 있는 정보와 함께 목록으로 표시된다.
- 검색 결과가 많을 경우를 대비해 목록은 최대 20건까지 표시하며, 그 이상은 페이지네이션으로 제공한다.
- 사용자는 검색 결과 목록에서 하나의 학교를 선택할 수 있다.

### 3.2 날짜 범위 선택

- 사용자는 조회할 급식 정보의 시작일과 종료일을 선택할 수 있다.
- 날짜 선택 UI는 달력 형태 등 직관적인 방식으로 제공한다.
- 조회 가능한 최대 날짜 범위는 **31일(1개월)**로 제한한다. 이는 결과 목록의 가독성과 응답 속도를 확보하기 위함이며, NEIS API의 1회 최대 응답 건수 제한(1,000건)과도 여유 있게 부합한다.

### 3.3 급식(중식) 조회 결과 표시

- 선택된 학교와 날짜 범위에 해당하는 날짜별 중식 급식 메뉴를 순서대로 표시한다.
- 각 날짜별로 메뉴 이름, 칼로리 정보 등 NEIS API에서 제공하는 정보를 함께 표시한다.

### 3.4 오늘의 왕 (급식 배틀 토너먼트) — 추가 기능

- 메인 페이지 상단에 "오늘의 왕"으로 선정된 학교와 급식 메뉴를 노출한다.
- 매일 자동으로 전국 고등학교 중 128개교를 지역(시도교육청)별로 균등하게 추출하여, 해당 학교들의 당일 중식 메뉴를 대상으로 싱글 엘리미네이션 토너먼트(128강 → 64강 → 32강 → 16강 → 8강 → 4강 → 2강 → 우승)를 진행한다.
- 각 대결은 "건강 점수" 자동 채점 알고리즘으로 승부를 가른다. 채점 기준은 다음을 포함한다.
  - 제철 재료 포함 여부 (키워드 매칭 기반 추정)
  - 탄수화물·단백질·지방(탄단지) 비율의 적정성
  - 반찬 수(메뉴 가짓수)에 따른 가산점
  - 인스턴트/가공식품 포함 시 감점
- 채점 기준의 세부 항목과 배점은 `docs/SCORING_ALGORITHM.md` 문서에 정의하며, 채점 로직 코드가 변경될 때마다 이 문서도 함께 갱신되어야 한다(문서-코드 동기화 원칙).
- 그날의 토너먼트 결과 우승 학교가 "오늘의 왕"으로 메인 페이지에 표시된다.
- 사용자는 오늘의 왕 학교의 급식 메뉴와 채점 근거(항목별 점수)를 함께 확인할 수 있다.
- **참가 학교 결측 처리**: 추출된 128개교 중 당일 중식 급식 정보가 등록되지 않은 학교가 있는 경우, 같은 지역 배분 몫에서 급식 정보가 확인되는 학교로 즉시 대체 추첨한다. 대체 가능한 학교가 지역 내에 없으면 다른 지역에서 보충하며, 그래도 128개를 채우지 못하면 128 이하의 가장 가까운 2의 거듭제곱(64강 등)으로 대진표 규모를 자동 축소한다.

---

## 4. 예외 상황 및 처리

| 상황 | 처리 방식 |
|------|-----------|
| **검색 결과 없음** | 입력한 검색어와 일치하는 학교가 없을 경우, 사용자에게 "검색 결과가 없습니다" 등의 안내 메시지를 표시한다. |
| **잘못된 날짜 범위** | 종료일이 시작일보다 빠르거나, 최대 허용 범위(31일)를 초과하는 경우, 사용자에게 올바른 날짜 범위를 입력하도록 안내 메시지를 표시한다. |
| **급식 정보 없음** | 선택한 학교와 날짜 범위에 급식 정보가 존재하지 않는 경우(공휴일, 방학 등), 해당 날짜에 급식 정보가 없다는 안내를 표시한다. |
| **오늘의 왕 데이터 없음** | 당일 토너먼트 배치가 아직 실행되지 않았거나 실패한 경우, 메인 페이지에는 "오늘의 왕 준비 중"과 같은 안내를 표시하고 이전 앱 사용에는 영향을 주지 않는다. |
| **NEIS API 응답 오류** | 외부 API 호출이 실패하거나 응답이 지연되는 경우, 사용자에게 일시적인 오류 안내를 표시하고 재시도를 유도한다. |

---

## 5. 범위 외 사항 (Out of Scope)

- 조식/석식 등 중식 외 급식 정보 조회는 이번 범위에 포함하지 않는다.
- 사용자 계정/로그인 기능은 이번 범위에 포함하지 않는다.
- 오늘의 왕 결과에 대한 사용자 투표 기능은 이번 범위에 포함하지 않으며, 추후 기능 확장으로 고려한다.

---

## 6. 성공 지표 (KPI)

앱의 목적 달성 여부를 판단하기 위해 아래 지표를 참고 지표로 삼는다. MVP 단계에서는 별도 분석 도구를 도입하지 않되, 추후 지표 수집이 가능하도록 이벤트 로그(방문, 검색, 오늘의 왕 조회) 설계를 고려한다.

| 지표 | 설명 |
|------|------|
| 일간 순 방문자 수(DAU) | 앱 메인 페이지 방문 횟수 |
| 오늘의 왕 조회율 | 방문자 중 "오늘의 왕" 섹션을 클릭/조회한 비율 |
| 급식 조회 완료율 | 학교 검색을 시작한 세션 중 실제로 급식 결과 조회까지 도달한 비율 |
| 재방문율 | 동일 사용자가 다음날 이후 다시 방문하는 비율 |
