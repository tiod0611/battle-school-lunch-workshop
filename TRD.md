# TRD (Technical Requirements Document)

## 1. 개요

본 문서는 `PRD.md`에서 정의한 "급식 배틀 - 학교 급식 조회 앱"의 기술적인 설계와 구현 방침을 정의한다.

---

## 2. 시스템 구성

애플리케이션은 **프론트엔드 앱**과 **백엔드 앱**의 2개 컴포넌트로 구성되며, **Docker Compose**로 함께 오케스트레이션한다.

```
┌─────────────────┐        REST API        ┌──────────────────┐        REST API        ┌─────────────┐
│  Frontend (React) │ ─────────────────────▶ │  Backend (Python)  │ ─────────────────────▶ │  NEIS Open  │
│  UI 렌더링만 담당   │ ◀───────────────────── │  FastAPI            │ ◀───────────────────── │  API        │
└─────────────────┘   src/openapi.json 기반   └──────────────────┘   data/openapi.json 기반  └─────────────┘
                        생성된 클라이언트 사용                          생성된 클라이언트 사용
```

### 2.1 프론트엔드 책임 (React)

- 사용자 인터페이스(UI/UX)만 담당한다.
- **NEIS API를 직접 호출하지 않는다.** 모든 데이터는 반드시 백엔드 API를 통해서만 조회한다.
- 학교 검색 → 날짜 선택 → 결과 표시의 3단계 화면 흐름을 구현한다.
- 메인 페이지 상단에 "오늘의 왕" 정보를 표시하는 컴포넌트를 포함한다.
- 프론트엔드-백엔드 간 통신은 `src/openapi.json`(백엔드 API 명세)을 기준으로 생성/구현한 API 클라이언트 코드를 통해서만 이루어진다. 엔드포인트나 요청/응답 스키마를 임의로 하드코딩하지 않는다.

### 2.2 백엔드 책임 (Python, FastAPI)

- NEIS Open API(`data/openapi.json`에 정의된 학교기본정보, 급식식단정보 API)를 호출하여 결과를 가공한 뒤 프론트엔드에 전달한다.
- `data/openapi.json` 명세에 정의된 엔드포인트, 파라미터, 응답 스키마를 근거로 NEIS API와 통신하는 클라이언트 코드를 구현한다(직접 문자열 URL 조립 등 명세와 어긋나는 방식 지양).
- 프론트엔드에 제공할 자체 API 엔드포인트와 데이터 스키마를 `src/openapi.json`으로 별도 정의하고, 이 명세를 기준으로 서버 라우트 및 프론트엔드 클라이언트 코드를 구현한다.
- 오늘의 왕 토너먼트를 위한 일일 배치 작업(학교 추출, 채점, 대진, 결과 저장)을 수행한다.

### 2.3 데이터 흐름

1. 프론트엔드가 백엔드의 `학교 검색 API`를 호출한다.
2. 백엔드는 `data/openapi.json`의 학교기본정보 API 클라이언트를 통해 NEIS API를 호출하고, 부분 일치하는 학교 목록을 가공하여 프론트엔드에 반환한다.
3. 사용자가 학교와 날짜 범위를 선택하면, 프론트엔드는 백엔드의 `급식 조회 API`를 호출한다.
4. 백엔드는 `data/openapi.json`의 급식식단정보 API 클라이언트를 통해 NEIS API를 호출하고, 날짜별 중식 메뉴만 필터링하여 프론트엔드에 반환한다.
5. 오늘의 왕 배치는 백엔드 내부 스케줄러가 매일 실행하며, 결과를 데이터베이스(SQLite)에 저장한다. 프론트엔드는 `오늘의 왕 조회 API`를 통해 최신 결과를 가져온다.

---

## 3. 기술 스택

| 영역 | 기술 |
|------|------|
| 프론트엔드 | React, TypeScript, Vite |
| 백엔드 | Python, FastAPI |
| 데이터베이스 | SQLite |
| 컨테이너 오케스트레이션 | Docker Compose |
| 프론트엔드 테스트 | 통합 테스트 (예: React Testing Library + MSW) |
| 백엔드 테스트 | 단위 테스트 및 통합 테스트 (pytest) |
| E2E 테스트 | Playwright |

> 프레임워크 및 라이브러리의 특정 버전은 구현 단계에서 `requirements.txt`/`package.json`에 명시한다.

---

## 4. 백엔드 API 설계 (`src/openapi.json` 기준 초안)

백엔드는 프론트엔드-백엔드 계약을 `src/openapi.json`(OpenAPI 3.0.4)으로 정의한다. 아래는 초기 설계 초안이며, 실제 구현 시 `src/openapi.json`을 단일 진실 공급원(source of truth)으로 삼는다.

| 메서드 | 경로 | 설명 |
|--------|------|------|
| `GET` | `/api/schools/search?keyword={keyword}` | 학교 이름 일부(keyword)로 학교를 검색하여 목록 반환 |
| `GET` | `/api/meals?schoolCode={schoolCode}&officeCode={officeCode}&from={date}&to={date}` | 특정 학교의 날짜 범위 내 날짜별 중식 급식 정보 반환 |
| `GET` | `/api/tournament/today` | 오늘의 왕 토너먼트 결과(우승 학교, 급식 메뉴, 채점 근거) 반환 |

각 엔드포인트는 요청 파라미터(이름/위치/필수여부/타입/설명)와 성공/오류 응답(상태 코드, 콘텐츠 타입, 스키마)을 `src/openapi.json`에 명시한다.

---

## 5. 오늘의 왕 (급식 배틀 토너먼트) 기술 설계

### 5.1 대상 학교 추출

- 매일 1회, 전국 고등학교 중 128개교를 시도교육청(지역)별로 균등하게 무작위 추출한다.

### 5.2 토너먼트 진행

- 추출된 128개교를 대상으로 싱글 엘리미네이션 방식의 대진표를 구성한다 (128강 → 64강 → 32강 → 16강 → 8강 → 4강 → 2강 → 우승, 총 7라운드).
- 각 대결은 두 학교의 당일 중식 메뉴에 대한 "건강 점수"를 비교하여 높은 점수의 학교가 승리한다.

### 5.3 건강 점수 채점 알고리즘

- 채점은 아래 4개 항목을 기준으로 한다.
  1. **제철 재료 반영도**: 메뉴명 텍스트에서 계절별 제철 재료 키워드 매칭 여부로 가점 산정
  2. **탄단지 비율 적정성**: NEIS 급식 API의 영양소 정보(탄수화물/단백질/지방)를 기준으로 적정 비율에 가까울수록 가점
  3. **반찬 수(메뉴 다양성)**: 메뉴 항목 개수에 비례하여 가점
  4. **인스턴트/가공식품 포함 여부**: 가공식품·인스턴트 관련 키워드가 포함된 메뉴가 있을 경우 감점
- 채점 알고리즘의 세부 기준(키워드 목록, 배점, 가중치 등)은 `docs/SCORING_ALGORITHM.md`에 문서화한다.
- **문서-코드 동기화 원칙**: 채점 로직 코드가 변경되면 반드시 `docs/SCORING_ALGORITHM.md`를 함께 갱신하고, 문서 내용이 변경되면 코드에도 동일하게 반영한다. 코드 리뷰 시 두 파일의 변경 이력이 일치하는지 확인한다.

### 5.4 배치 실행 및 데이터 저장

- 백엔드 내 스케줄러(예: APScheduler)를 사용하여 매일 정해진 시각에 배치 작업을 실행한다.
- 배치 실행 결과(참가 학교 목록, 대진표, 라운드별 결과, 최종 우승 학교, 채점 상세)는 SQLite 데이터베이스에 저장한다.
- 프론트엔드는 `/api/tournament/today` 엔드포인트를 통해 최신 배치 결과만 조회하며, 배치 로직을 직접 트리거하지 않는다.

---

## 6. Docker Compose 구성

- 프론트엔드와 백엔드는 각각 독립된 컨테이너로 빌드된다.
- 루트의 `docker-compose.yml`에서 두 서비스를 정의하고, 프론트엔드는 백엔드 서비스에 네트워크로 접근할 수 있도록 구성한다.
- 백엔드 서비스는 SQLite 데이터 파일을 유지하기 위한 볼륨을 마운트한다.
- 로컬 개발 및 실행에 필요한 환경 변수(NEIS API 키 등)는 `.env` 파일을 통해 주입하며, `.env.example`로 예시를 제공한다.

```
services:
  frontend:
    build: ./frontend
    ports: ["3000:3000"]
    depends_on: [backend]
  backend:
    build: ./backend
    ports: ["8000:8000"]
    volumes: ["./backend/data:/app/data"]
    env_file: .env
```

> 위 구성은 개념적인 초안이며, 실제 구현 단계에서 세부 사항(네트워크, 헬스체크 등)을 구체화한다.

---

## 7. 테스트 전략

### 7.1 프론트엔드 테스트

- **통합 테스트만 작성한다.** 개별 컴포넌트 단위 테스트 대신, 화면 단위(학교 검색, 날짜 선택, 결과 표시, 오늘의 왕 표시)의 사용자 상호작용을 검증하는 통합 테스트를 작성한다.
- 백엔드 API는 목(mock) 또는 테스트 서버로 대체하여 프론트엔드 로직만 검증한다.

### 7.2 백엔드 테스트

- **단위 테스트**: 학교 검색 로직, 급식 필터링 로직, 건강 점수 채점 알고리즘 등 핵심 함수 단위로 작성한다.
- **통합 테스트**: API 엔드포인트 단위로 요청-응답 전체 흐름(NEIS API는 목 처리)을 검증한다.

### 7.3 E2E 테스트

- 실제 브라우저 환경에서 "학교 검색 → 학교 선택 → 날짜 범위 선택 → 급식 결과 확인"의 전체 사용자 흐름을 검증한다.
- 메인 페이지의 "오늘의 왕" 표시 및 상세 정보 확인 흐름도 E2E 테스트 범위에 포함한다.
- Playwright 등 E2E 테스트 도구를 사용하며, Docker Compose로 띄운 환경을 대상으로 실행하는 것을 원칙으로 한다.

---

## 8. 제약 사항 및 원칙

- 프론트엔드는 어떠한 경우에도 NEIS API를 직접 호출하지 않는다.
- 백엔드-NEIS 간 통신은 `data/openapi.json` 명세를, 프론트엔드-백엔드 간 통신은 `src/openapi.json` 명세를 단일 진실 공급원으로 삼는다.
- 오늘의 왕 채점 알고리즘 코드와 `docs/SCORING_ALGORITHM.md` 문서는 항상 동기화 상태를 유지한다.
